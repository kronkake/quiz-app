import React from 'react';

const Header = () => (
  <header className="header-top">
    <div className="headertext-Container">
      <strong style={{color: "white", fontSize: 20}}>QuizApp</strong>
    </div>
  </header>
);

export default Header;
